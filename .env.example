# Postgres (Neon on Vercel in production). Real values live in
# .env.local, which `vercel env pull` regenerates and is gitignored.
DATABASE_URL="postgresql://user:password@host/dbname?sslmode=require"
